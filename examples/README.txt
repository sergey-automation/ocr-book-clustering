This is an example of UMAP visualization generated by the pipeline.

Each point represents a book embedding.
Clusters correspond to semantic topics.

- Cluster -1 is noise (hidden by default)
- Hover over points to see book metadata
- Table below shows cluster statistics